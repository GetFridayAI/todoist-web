# macOS Skeleton

This folder is reserved for macOS-specific native implementation details.

Suggested next steps:

1. Run `npm run prebuild:ios`.
2. Open the generated iOS workspace in Xcode.
3. Add a Mac Catalyst target or integrate `react-native-macos`.
4. Place native notes, target settings, and platform files in this folder.
