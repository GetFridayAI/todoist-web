import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  light: {
    backgroundColor: '#fff',
    width: '100%',
    height: '100%',
    color: '#000',
  },
  dark: {
    backgroundColor: '#000',
    width: '100%',
    height: '100%',
    color: '#fff',
  },
});

export default styles;
