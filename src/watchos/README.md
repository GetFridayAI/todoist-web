# watchOS Skeleton

This folder is reserved for watchOS-specific code and documentation.

Suggested next steps:

1. Run `npm run prebuild:ios`.
2. Open the generated iOS project in Xcode.
3. Add a watchOS companion app and extension target.
4. Keep SwiftUI watch implementation notes/files in this folder.
